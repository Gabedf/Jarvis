from Data import options as op

# OPÇÕES
def firstOptions():
    for i in range(len(op.start)):
        print(op.start[i])
    print('\n')

def talkOptions():
    for i in range(len(op.start)):
        print(op.talk[i])